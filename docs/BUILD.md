# Build and development

## Local development

Clone the repository, install dependencies with **yarn**, then **`yarn build`** produces `dist/whisker.js`.

Unit tests use **Mocha**: run **`yarn test`**.

## Releases

Release artifacts include **`dist/whisker.js`** for HACS or manual installation under `www/community/whisker/`.
