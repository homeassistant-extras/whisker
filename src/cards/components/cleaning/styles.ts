import { css } from 'lit';

export const cleaningStyles = css`
  :host {
    display: contents;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    padding-left: 4px;
    cursor: pointer;
  }
`;
