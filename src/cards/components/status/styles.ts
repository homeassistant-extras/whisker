import { css } from 'lit';

export const statusStyles = css`
  :host {
    display: flex;
    align-items: center;
  }

  .status-icon-wrap {
    display: inline-flex;
    align-items: center;
    padding-left: 3px;
    cursor: pointer;
  }
`;
