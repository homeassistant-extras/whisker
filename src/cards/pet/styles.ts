import { css } from 'lit';

export const styles = css`
  :host {
    display: block;
  }

  .card-title {
    font-size: 1.25rem;
    font-weight: 500;
    margin: 0;
    padding: 10px 20px;
    border-bottom: 1px solid var(--divider-color);
  }

  whisker-pet-graph {
    display: block;
    padding-bottom: 8px;
  }
`;
